/target
/data

# Holds a live shared secret; copy from gateway.Caddyfile.example.
/deploy/gateway.Caddyfile

# The Node bundle stays local. The browser wasm-bindgen output is committed so
# Netlify can publish it without the workspace's sibling Rust dependencies.
/client/pkg-node/
